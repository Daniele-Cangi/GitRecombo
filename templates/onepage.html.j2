<!doctype html>
<html><head><meta charset="utf-8"/>
<title>{{ title }}</title>
<meta name="viewport" content="width=device-width, initial-scale=1"/>
<style>
 body{font-family:Inter,system-ui,Arial;margin:24px;line-height:1.45}
 table{border-collapse:collapse;width:100%;margin:12px 0}
 th,td{border:1px solid #ddd;padding:8px;text-align:left}
 code,pre{background:#fafafa;border:1px solid #eee;padding:8px;border-radius:6px;display:block;overflow:auto}
 .muted{color:#666}.pill{padding:2px 6px;border-radius:6px;border:1px solid #eee;background:#f9f9f9}
 .small{font-size:12px}.mono{font-family:ui-monospace,Menlo,Consolas,monospace}
 .grid{display:grid;gap:12px;grid-template-columns:1fr 1fr}
</style></head>
<body>
<h1>{{ title }}</h1><p class="muted">{{ summary }}</p>
<div class="grid">
  <div>
    <h3>Sources</h3>
    <table><thead><tr><th>Repo</th><th>License</th><th>Role</th><th>Nv</th><th>Rel</th><th>Hlth</th><th>Rep</th></tr></thead>
    <tbody>
    {% for s in sources %}
      <tr>
        <td><a href="{{ s.url }}" target="_blank" rel="noopener">{{ s.name }}</a><br>
            <span class="small muted mono">{{ (s.concepts or []) | join(", ") }}</span></td>
        <td><span class="pill">{{ s.license or 'N/A' }}</span></td>
        <td>{{ s.role }}</td>
        <td>{{ '%.2f' % (s.novelty_score or 0.0) }}</td>
        <td>{{ '%.2f' % (s.relevance or 0.0) }}</td>
        <td>{{ '%.2f' % (s.health_score or 0.0) }}</td>
        <td>{{ '%.2f' % (s.author_rep or 0.0) }}</td>
      </tr>
    {% endfor %}
    </tbody></table>
  </div>
  <div>
    <h3>Architecture</h3>
    <pre><code>{{ architecture_ascii }}</code></pre>
  </div>
</div>
<h3>Seed (commands)</h3><pre><code>{% for c in seed_commands %}{{ c }}
{% endfor %}</code></pre>
<h3>Project layout</h3><pre><code>{% for p in project_tree %}{{ p }}
{% endfor %}</code></pre>
<h3>Why it works</h3><ul>{% for w in why_it_works %}<li>{{ w }}</li>{% endfor %}</ul>
<p class="small muted">Generated by GitRecombo</p>
</body></html>
